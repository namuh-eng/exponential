{
  "name": "exponential-monorepo",
  "version": "0.1.0",
  "private": true,
  "packageManager": "bun@1.3.14",
  "scripts": {
    "dev": "bun run --filter @exponential/web dev",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "test:e2e": "bun run --filter @exponential/web test:e2e",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "cpd": "bun run --filter @exponential/web cpd",
    "api:build": "cd apps/api && go build ./...",
    "api:test": "cd apps/api && go test ./...",
    "web-api-empty": "node scripts/check-web-api-empty.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "turbo": "^2.6.1"
  },
  "workspaces": ["apps/*", "packages/*"],
  "overrides": {
    "postcss": "^8.5.15"
  }
}
