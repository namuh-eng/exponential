{
  "name": "exponential-monorepo",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.24.0",
  "scripts": {
    "dev": "pnpm --filter @exponential/web dev",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "test:e2e": "pnpm --filter @exponential/web test:e2e",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "cpd": "pnpm --filter @exponential/web cpd",
    "db:generate": "pnpm --filter @exponential/web db:generate",
    "db:migrate": "pnpm --filter @exponential/web db:migrate",
    "db:push": "pnpm --filter @exponential/web db:push",
    "api:build": "cd apps/api && go build ./...",
    "api:test": "cd apps/api && go test ./..."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "turbo": "^2.6.1"
  }
}
